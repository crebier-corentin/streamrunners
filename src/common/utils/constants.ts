import { join } from 'path';

/**
 * Absolute path to the views directory.
 */
export const VIEW_DIR_PATH = join(__dirname, '..', '..', '..', 'views');
/**
 * Absolute path to the public directory.
 */
export const PUBLIC_DIR_PATH = join(__dirname, '..', '..', '..', 'public');
